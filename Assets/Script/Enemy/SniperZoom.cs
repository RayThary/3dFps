using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SniperZoom : MonoBehaviour
{
    [SerializeField]
    private bool isEquipped = false;
    public bool SetEquipped { set { isEquipped = value; } }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
